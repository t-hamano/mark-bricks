# @mark-bricks/fixtures

> [!NOTE]
> This is a private package. `@mark-bricks/fixtures` is not published to npm. It lives in this monorepo and is consumed by the hosts via pnpm's `workspace:` protocol

A package for managing the Markdown data used by Storybook, the round-trip tests, manual smoke tests, and the like.
