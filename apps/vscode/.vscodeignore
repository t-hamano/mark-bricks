# Everything the extension needs at runtime is bundled into dist/ by
# scripts/build.mjs, so only that and the package metadata ship.
.vscode/**
.vscode-test/**
src/**
scripts/**
node_modules/**
tsconfig*.json
vite.config.ts
.npmrc
**/*.map
**/*.ts
**/*.tsx
