// MarkBricks VSCode extension entry point.
//
// TODO: register CustomTextEditorProvider, host the @mark-bricks/editor webview,
// wire postMessage bridge for markdown value <-> document text, and expose
// markBricks.openVisual / markBricks.openText commands.

export function activate(): void {}

export function deactivate(): void {}
