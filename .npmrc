message=chore: release v%s

# Hoist all dependencies into the workspace-root node_modules (npm-style
# layout). Required so the Storybook Vite builder can resolve
# `storybook/internal/*` from its virtual modules, and to avoid
# phantom-dependency breakage in @wordpress/* packages.
shamefully-hoist=true
